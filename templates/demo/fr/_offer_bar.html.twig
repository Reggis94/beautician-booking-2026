<aside class="demo-offer-bar" aria-label="Accès à la plateforme Glam4Style">
  <p class="demo-offer-bar__text">
    Pour accéder à la plateforme, écrivez <strong>INTERESSE</strong> en commentaire sous notre dernière publication TikTok.
  </p>
</aside>

<style>
  .demo-offer-bar {
    align-items: center;
    background: #1C1916;
    bottom: 0;
    box-shadow: 0 -8px 28px rgba(28, 25, 22, .2);
    color: #FAF6F1;
    display: flex;
    gap: 24px;
    justify-content: center;
    left: 0;
    padding: 12px 24px;
    position: fixed;
    right: 0;
    z-index: 40;
  }
  .demo-offer-bar__text {
    font-family: 'DM Sans', system-ui, sans-serif;
    font-size: 20px;
    font-weight: 400;
    line-height: 1.2;
    margin: 0;
  }
  @media (max-width: 600px) {
    .demo-offer-bar {
      gap: 12px;
      justify-content: space-between;
      padding: 10px 14px;
    }
    .demo-offer-bar__text { font-size: 15px; }
  }
</style>
